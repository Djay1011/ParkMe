<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Bottom layer: solid color fill -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="?attr/colorSurface" />
            <corners android:radius="1dp" />
        </shape>
    </item>

    <!-- Top layer: bottom stroke -->
    <item android:bottom="-2dp" android:right="-2dp" android:left="-2dp">
        <shape android:shape="rectangle">
            <stroke android:width="1dp" android:color="@color/blue" />
            <!-- Transparent fill to make only the stroke visible -->
            <solid android:color="@android:color/transparent" />
            <!-- No need to define corners here unless you want a specific effect -->
        </shape>
    </item>
</layer-list>
