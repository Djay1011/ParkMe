<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for Material 3 -->
    <style name="Base.Theme.ParkMe" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your theme here using Material 3 color system. -->

        <!-- Primary color (used for app bars, buttons, etc.) -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryContainer">@color/blueLight</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary color (used for floating action buttons, selection controls, etc.) -->
        <item name="colorSecondary">@color/green</item>
        <item name="colorSecondaryContainer">@color/greenLight</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Background color -->
        <item name="android:colorBackground">@color/light_grey</item>
        <item name="colorOnBackground">@color/black</item>

        <!-- Error color -->
        <item name="colorError">@color/muted_red</item>
        <item name="colorOnError">@color/white</item>

        <!-- Surface color -->
        <item name="colorSurface">@color/off_white</item>
        <item name="colorOnSurface">@color/black</item>


        <item name="shapeAppearanceSmallComponent">@style/ShapeAppearance.Material3.SmallComponent</item>
        <!-- ... other shape appearances ... -->
    </style>

    <style name="Theme.ParkMe" parent="Base.Theme.ParkMe" />
</resources>